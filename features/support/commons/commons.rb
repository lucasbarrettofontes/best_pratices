# Classe metodos comuns
class Commons < SitePrism::Page
  # def take_screenshot(_filename, _result)
  #   timerpath = Time.now.strftime('%Y_%m_%d').to_s
  #   filepath = 'screenshots/test_# {_result}/run_# {timerpath}'
  #   screenshot = '# {filepath}/# {_filename}.png'
  #   page.save_screenshot(screenshot)
  #   embed(screenshot, 'image/png', 'SCREENSHOT')
  # end
end
